---
import BentoBase from "./BentoBase.astro";
import { type BentoLayouts } from "../types/bento";
interface Props {
    layout: BentoLayouts
}

const { ...rest } = Astro.props;
---

<BentoBase
    class="gap-5 flex flex-col items-center justify-center text-xl p-5 border-1 rounded-4xl border-border  h-full bg-bg"
    {...rest}
>
    <slot />
</BentoBase>
