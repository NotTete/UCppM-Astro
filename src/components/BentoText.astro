---
import BentoBase from "./BentoBase.astro";
interface Props {
    position: [number, number];
    span?: [number, number];
}

const { ...rest } = Astro.props;
---

<BentoBase
    class="gap-5 flex flex-col items-center justify-center text-xl p-5 border-1 rounded-4xl border-border bg-bg h-full"
    {...rest}
>
    <slot />
</BentoBase>
