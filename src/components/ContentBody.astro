---

const { class: className } = Astro.props;
---
<main class={"w-full flex flex-col items-center justify-center py-20 lg:max-w-5xl md:max-w-3xl m-auto " + (className ? className : "")}>
    <slot/>
</main>