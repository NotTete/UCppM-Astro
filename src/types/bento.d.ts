export type BentoLayout = [number, number, number, number];

export type BentoLayouts = {
    sm?: BentoLayout;
    md?: BentoLayout;
    lg?: BentoLayout;
    default?: BentoLayout;
}

export type BentoMatrix = [number, number];

export type BentoMatrices = {
    sm?: BentoMatrix;
    md?: BentoMatrix;
    lg?: BentoMatrix;
    default?: BentoMatrix;
}
